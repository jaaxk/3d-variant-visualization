/* Minimal stub standing in for the real 3Dmol.min.js in offline tests.
 * Only its presence/content-inlining is exercised here, not real WebGL
 * rendering (that would require a browser, out of scope for pytest). */
var $3Dmol_test_stub_marker = "THIS_IS_THE_STUB_3DMOL_JS";
